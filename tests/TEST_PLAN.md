# Test Plan

Current automated tests cover the tarot domain only.

- Unit tests: `tests/test_tarot_card.py`, `tests/test_spread.py`
- Smoke tests: `test_basic.py`

Run all tests with:

```bash
python -m pytest -q
```
